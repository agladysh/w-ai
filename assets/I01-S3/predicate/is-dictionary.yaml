name: is-dictionary
description: if input is a dictionary value, returns true, otherwise returns false
input: unknown
output: boolean
process:
  - . ts: | # ts
      export default ($: unknown): boolean => typeof($) === 'object' && $ !== null;
