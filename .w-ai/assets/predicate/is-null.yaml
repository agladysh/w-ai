name: is-null
description: if input is a null value, returns true, otherwise returns false
input: unknown
output: boolean
process:
  - ts: | # ts
      export default ($: unknown): boolean => $ === null;
