name: is-non-empty-string
description: if input is a non-empty string value, returns true, otherwise returns false
input: string
output: string
process:
  - ts: | # ts
      export default ($: string): boolean => $.length > 0;
